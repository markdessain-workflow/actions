# action.yml
name: 'download_binary'
description: ''
inputs:
  name:
    description: ''
    required: true
  token:
    description: ''
    required: true
  path:
    description: ''
    required: false
    default: './bin'
outputs:
  binary:
    description: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.token }}
    - ${{ inputs.name }}
    - ${{ inputs.path }}