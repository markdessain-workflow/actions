# action.yml
name: 'download_binary'
description: ''
inputs:
  name:
    description: ''
    required: true
  path:
    description: ''
    required: false
    default: './dist'
outputs:
  binary:
    description: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.name }}
    - ${{ inputs.path }}