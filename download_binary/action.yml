# action.yml
name: 'download_binary'
description: ''
inputs:
  name:
    description: ''
    required: true
  token:
    description: ''
    required: true
  path:
    description: ''
    required: false
    default: './dist'
outputs:
  binary:
    description: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.token }}
    - ${{ inputs.name }}
    - ${{ inputs.path }}
  entrypoint: '/entrypoint.sh'
  post-entrypoint: '/post-entrypoint.sh'